# Defense-in-depth: never run dependency lifecycle (install/postinstall) scripts.
# The tree has none today; this keeps it that way if a future dep adds one.
# Explicit `npm test` / `npm run <script>` still run (only pre/post hooks skip).
ignore-scripts=true
